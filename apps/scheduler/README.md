# AI Scheduler Optimization Engine

This is the background solver worker microservice utilizing Google OR-Tools CP-SAT in Python.

## Core Setup
- Dependencies are declared in `requirements.txt`.
- Solver loops are triggered asynchronously via Redis.
