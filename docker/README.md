# Docker Configurations

This directory will hold all container configurations for local execution and production deployment.

## Contents
- `docker-compose.yml`: Launches MongoDB replica set, Redis queue instance, Node.js API Gateway, and Python Worker.
- `Dockerfile` definitions inside each app service.
