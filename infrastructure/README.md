# Infrastructure Configuration

This folder stores infrastructure as code (IaC) templates for deployment.

## Contents
- `terraform/`: AWS Fargate ECS task definitions, Redis Cache cluster, DocumentDB cluster.
- `kubernetes/`: Service manifests and auto-scaling rules.
